<Page x:Class="Практическая4_Русаков_Худайбердин.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Практическая4_Русаков_Худайбердин"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
       Source="/Images/img1.jpg" 
       Height="100" 
       Margin="5" 
       Stretch="Uniform"
       HorizontalAlignment="Center"
       ToolTip="Формула для вычисления Вариант 2"/>

        <Label Grid.Row="1" Grid.Column="0" Content="X:" 
               ToolTip="Введите значение переменной X"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TextBoxX" 
                 Margin="5" ToolTip="Поле для ввода X"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Y:" 
               ToolTip="Введите значение переменной Y"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TextBoxY" 
                 Margin="5" ToolTip="Поле для ввода Y"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Z:" 
               ToolTip="Введите значение переменной Z"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TextBoxZ" 
                 Margin="5" ToolTip="Поле для ввода Z"/>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="Вычислить" Width="100" Margin="5" 
                    Click="ButtonCalculate_Click" 
                    ToolTip="Нажмите для расчета функции"/>
            <Button Content="Очистить" Width="100" Margin="5" 
                    Click="ButtonClear_Click" 
                    ToolTip="Нажмите для очистки полей"/>
        </StackPanel>

        <Label Grid.Row="5" Grid.Column="0" Content="Результат:" 
               VerticalAlignment="Center" ToolTip="Поле вывода результата"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="TextBoxResult" 
                 Margin="5" IsReadOnly="True" 
                 ToolTip="Здесь отобразится результат вычисления"/>
    </Grid>
</Page>
